{
  "name": "@leminejs/website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "dev": "vite --host 0.0.0.0",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint . --ext .ts,.tsx",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@leminejs/core": "workspace:^",
    "@leminejs/widgets": "workspace:^"
  },
  "devDependencies": {
    "@leminejs/vite-plugin": "workspace:^"
  }
}
